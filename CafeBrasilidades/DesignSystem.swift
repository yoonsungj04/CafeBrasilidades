import SwiftUI

extension Color {
    // Warm pastel beige — the canvas background
    static let cafeBg = Color(red: 0.961, green: 0.944, blue: 0.920)
    // Deep warm brown — text, icons
    static let cafeText = Color(red: 0.28, green: 0.20, blue: 0.14)
    // Soft muted terracotta accent
    static let cafeAccent = Color(red: 0.72, green: 0.52, blue: 0.38)
}
